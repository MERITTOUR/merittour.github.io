# merittour.github.io — 프로젝트 컨텍스트

메리트투어(MERITTOUR) GitHub Pages 사이트입니다. 정적 HTML/CSS로 구성되며,
`main` 브랜치 기준으로 GitHub Pages에 배포됩니다.

## 작업 규칙

### 표준 워크플로우 (중요)
모든 변경은 아래 순서를 별도 확인 없이 끝까지 진행한다.

1. **작업 브랜치 생성** — `main` 최신에서 작업용 브랜치(예: `claude/*`)를 만든다(또는 동기화).
2. **검증** — 변경 후 정적 검증으로 깨진 곳이 없는지 확인한다: `<div>` 등 태그 균형, 사용 클래스의 CSS 누락 여부, 링크/경로, base64·콘텐츠 보존, 가능하면 렌더 확인.
3. **커밋·푸시** — 명확한 한국어 메시지로 커밋하고 작업 브랜치에 푸시한다.
4. **PR 생성** — base `main` 으로 PR을 생성한다.
5. **Squash 머지** — PR을 **squash 방식**으로 머지한다(커밋 1개로 정리).
6. **결과 보고** — PR/머지 링크와 반영 URL을 보고하고, **필요 시 되돌리는 법(revert)** 도 함께 안내한다.

- 예외: 사용자가 "PR만", "머지하지 마"라고 하면 5번에서 멈춘다.

### 커밋
- committer: `Claude <noreply@anthropic.com>`
- 커밋 메시지는 한국어로 명확하게 작성한다.

## 사이트 구조 (주요)

```
/                          (임시) 회사소개로 리다이렉트 — 게이트 디자인은 git 이력에 보존
/about/                    회사소개 (루트 고정 주소 — 이동 금지)
  docs/index.html          가입 서류 작성 안내 — EWRC Ⅱ · 쿠쥬 타운하우스(개인 · 법인) 탭. 서류마다 「채우실 곳」만
                           빨간 테두리 서식 흉내 + 파란 가상 예시(홍길동) · 준비물 ⓐ~ · 절차 1~7 · 입금 · 은행 CDD·EDD · 보내실 곳 (noindex)
/guide/                    리조트 안내 허브 (야마나미·포틴힐즈 목록)
  index.html               허브 — 리조트 선택
  yamanami/                아소 야마나미 리조트 안내
    index.html             랜딩 — 안내 메뉴 바로가기
    assets/yamanami.css    공유 다크 테마 스타일시트 (랜딩·식사 공통)
    assets/photos/         메뉴 사진 (추가·안주 사진, 주간 저녁 포스터 등)
    dining/index.html      식사 안내 — 조식·중식·석식 순서
  14hills/                 나고야 포틴힐즈 CC 안내 (랜딩 + dining)
/2027/                     2027 시즌 골프 패키지 예약 안내 (손님 공지용 · 사내 도구함에서 이전)
  index.html               전 지역 상품·요금·예약 절차 · 상단 목차 칩(모바일)·카카오톡 상담·맨 위로
  photos/                  숙소·골프장 사진 (<지역>/<숙소폴더>|golf/01.webp 부터 번호를 이어서 · 장수 제한 없음)
                           ※ 원본은 merittour-tools 에 있었고, 구 주소는 리다이렉트만 남김
/reserve/                  회원 간편 예약 (프로토타입 · Supabase 연결 전 단계)
  index.html               랜딩 = 검색 4단계(어디서/어디로/얼마나/몇 명) + 지역별 상품 목록
                           + 같은 화면 상품 상세(좌 설명·일정표 / 우 예약 가능 달력) + 로그인·마이페이지·예약확인·고객센터
                           ※ 단일 HTML, 목 데이터 인라인(CAT·RES·TAGS·CHECKS·ROUND·DEMO_ACCOUNTS·DEMO_BOOKINGS)
                           ※ 우선예약 차수(PRIORITY_SCHEDULE) 누적·차감, `?sim=ISO` 로 시각 시뮬레이션
                           ※ 상품 데이터의 단일 출처 = 사내 「2027 시즌 골프투어 예약 안내」 가이드
                             (merittour-tools `tools/booking/2027guide/`) + 리조트 마스터 보유 객실
  spec/index.html          요구사항 정의서 (개발 위탁사 전달용 · A4 인쇄 CSS · noindex)
  book/index.html          구 다단계 예약 플로우 (레거시 · 어디에서도 링크하지 않음)
  confirm/index.html       모바일 확정서 예시 (섹션 자동 접힘·동일 일정 병합·결제 현황)
/assets/fresh.js           열어 둔 탭이 옛 안내문을 들고 있는 것을 막는 검사(손님 페이지 전부에 삽입)
```
- **새 페이지를 만들면 `</body>` 앞에 `<script defer src="/assets/fresh.js"></script>` 를 넣는다.**
  탭을 며칠 열어 두면 문서를 다시 요청하는 일이 없어 캐시 설정으로는 못 고친다(실제로 `/2027/`
  숙소 카드가 두 칸이던 시절 화면이 그대로 보였다). 돌아왔을 때 `HEAD` 로 ETag 만 물어보고,
  달라졌으면 띠를 띄운다. **저절로 새로고침하지 않는다** — 읽던 자리·고른 날짜가 날아간다.
  - 새로고침하면 손해인 화면이 있으면 그 페이지에서 `window.MT_FRESH_BUSY = () => boolean` 을
    정의한다(예: `/reserve/` 는 상품·시트·완료 화면, `/2027/` 은 사진 크게 보기 중).
  - `/reserve/spec/`·`/reserve/book/`·루트 리다이렉트에는 넣지 않는다.
- **`/reserve/`는 별도 디자인 축**: 회원 예약 프로토타입이라 야마나미 다크 골드 테마가 아니라 프로토타입 고유 팔레트(시니어 친화 고대비)를 그대로 유지한다. Supabase 연결 시 목 데이터를 실데이터로 교체 예정.
- **`/reserve/`에 실제 회원 개인정보(이름·휴대폰·생년월일·고객ID)를 넣지 않는다.** public 저장소이므로 시연은 `DEMO_ACCOUNTS`(HONG/KIM/CHOI + 가상 번호)로만 한다.
- **상품 구성을 바꿀 때는 `index.html`(CAT·RES·TAGS·CHECKS·ROUND)과 `spec/index.html`(§2 상품 표)을 함께 고친다.** 2027 시즌 미진행 상품(후쿠로다노타키CC·닐라이스프링스CC 호텔·스가다이라 맨션)은 제외된 상태다.
- 계층: **(임시 게이트)`/` → 리조트 안내(`/guide/`) → 각 리조트(`/guide/yamanami/`, `/guide/14hills/`)**.
- `/about/`(회사소개)은 **루트 고정 주소**라 `/guide/` 아래로 옮기지 않는다.
  - `/about/` 의 「주요 운영 지역」은 `/2027/` 과 같은 사실(홀 수·체류 요일·출발 공항·우선예약 조건)을 다시 적고 있다.
    **`/2027/` 을 바꾸면 `/about/` 도 같이 본다**(2026-09 에 한 달 늦게 따라간 것을 한꺼번에 맞췄다 — 27홀 표기·간지/시즈노야도 7박 요일·무츠키/후쿠로다 공항).
    계약서 전문 모달(EWRC Ⅱ 제1~12조 · 장기숙박 제1~9조+첨부 1-1)은 **v1.5 원문 그대로**라 표기 규칙을 적용하지 않는다
    (검사 `about.mjs` 도 이 두 모달은 빼고 본다).
  - **계약서 모달은 원문을 옮겨 적은 것이다(2026-09-08).** 그 전 판은 요약·의역이라 제2조 시설 목록(삿포로·닐라이·시티 골프투어 빠짐) ·
    제5조 6·7항 · 제8조 1·2·6항 · 제9조 명칭(한국회원예약관리사무소)이 원문과 달랐다. **고칠 때는 PDF 와 대조**하고 문장을 다듬지 않는다.
    장기숙박 안내 모달(`modal-longstay`)은 요약이라 우리 표기를 쓰되, 숫자(5년 · 1년 연장 · 60일/6개월 반환 · 쿠폰 15만엔 · 4인 2동)는 계약서와 같아야 한다.
  - **가입 서류 안내는 `/about/docs/`** — 옛 「서류 작성 안내」 모달(`modal-docguide`)은 없앴고 CDD·EDD 안내도 그리로 옮겼다.
    **예시 데이터는 전부 가상**(홍길동 · HONG GILDONG · 600515-1234567 · 010-1234-5678 · hong@example.com · 테헤란로 100 ·
    주식회사 예시상사 · 110111-1234567 · 123-45-67890). 실제 회원·직원 정보를 넣지 않는다. 회사 번호(02-365-9800 · 팩스 9801 ·
    국민은행 817201-04-109230)만 실제 값. 빨간 테두리 = PDF 작성방법본의 빨간 표시와 같은 자리.
    **날짜 칸(20 년 월 일)은 비워 둔다(2026-09-08 · Min)** — 빨간 표시 밖이라 「비워 두세요」로 적었다. 「적어도 된다」로 되돌리지 말 것. 법인 탭의 준비물(법인인감증명서 · 등기부등본 ·
    사업자등록증 · 주주명부 · 대표이사 여권·신분증 사본 · 은행 법인 CDD/EDD 는 지점 확인)은 법인용 PDF 기준.
  - **PDF 판본** — EWRC Ⅱ 회원가입계약서 v1.5(실제본 + 작성방법본) · 장기숙박 계약서 v1.5 **개인용 · 법인용**(둘 다 을 = 株式会社 SaiZen).
    T&G NETWORKS JAPAN · 아도디아빌딩 · 40구좌 · 2인 · 렌터카가 보이는 빨간 네모 작성 안내는 **옛 판**이라 참고하지 않는다.
- 식사 안내는 **점심·저녁 분리 없이 한 페이지**(`dining/`)에서 **조식 → 중식 → 석식** 순서로 안내한다.
- 모든 하위 페이지 좌측 하단에 플로팅 **뒤로가기 버튼**(`.top-back`, 상위 폴더로 이동).

## 야마나미 디자인 시스템 (다크 골드/포레스트 톤)

- 공통 스타일은 `guide/yamanami/assets/yamanami.css` 한 곳에서 관리한다. 페이지별 인라인 `<style>`을 늘리지 말 것. (14hills 랜딩도 이 파일을 참조)
- 컬러: 딥 포레스트 그린(`#0d1f15`/`#142c1f`) + 골드(`#C9A876`/`#E0C896`) + 와인레드 강조(`#8B3A2F`/`#C45A4A`) + 크림 텍스트(`#F2EBD5`)
- 폰트: Noto Serif JP / Noto Serif KR / Pretendard
- 페이지 컨테이너 `.wrap` 폭 760px, 골드 인셋 보더(`.wrap::before`)

## 야마나미 콘텐츠 메모

- **조식·중식**: 모두 **매일 뷔페식**. 면식(화·금) 운영은 **폐지**됨. **「한식 위주」 표기는 야마나미 조·중·석식 어디에도 쓰지 않는다**(2026-09 · Min — `/2027/`·`/reserve/`·`/guide/yamanami/dining/` 에서 뺐다. 포틴힐즈 조식의 표기는 그대로).
- **도착일 점심**: 인천 출발팀 = 카츠카레(라운딩 직행), 김해 출발팀 = 저녁 식사부터 일정 시작.
  ※ 손님에게 보이는 출발지 표기는 **공항 이름(인천 · 김해 · 대구)으로 통일**. 「서울 출발팀」·「부산 출발팀」은 쓰지 않는다.
  코드 내부 키(`ORIGINS[].id`, 상품 `dep:[...]`)는 `'서울'`/`'부산'`/`'대구'` 그대로 — 바꾸면 상품 매칭이 어긋난다.
- **석식(주간 저녁)**: 일 해물전골 / 월 일본산 삼겹살 / 화 매운탕 / 수 소불고기 코스 / 목 해물순두부찌개 / 금 일본산 삼겹살 / 토 흑돼지 샤브샤브. (요일별 HTML 표 `.dinner-list` + 요리 사진으로 안내. 사진은 포스터에서 추출해 `assets/photos/dinner/{sun..sat}.webp`로 보관. 원본 포스터 `weekly_dinner.webp`도 보존)
- 표기: 메리트투어 / MERITTOUR, 야마나미, 라운딩(○)/라운드(×), 바베큐(○)/바비큐(×), **호텔(○)/골프텔(×)**.
  - 「골프텔」은 쓰지 않는다(2026-09 · Min) — 후쿠로다노타키CC **호텔**, 확정서 예시의 야마나미 **호텔**.
    사내 도구의 엠클릭 별칭(`aliases` 의 「○○CC 골프텔」)은 엠클릭이 쓰는 표기라 그대로 둔다 — 붙여넣은 표를 맞추는 열쇠다.
- **골프장 정식 명칭 = 「아소 야마나미CC」**. 「야마나미CC」 단독 표기는 쓰지 않는다.
  리조트 이름(**아소 야마나미 리조트**)과 본관(**야마나미 호텔**)은 그대로 — 골프장이 아니다.
- **야마나미 리조트 안 숙소 이름 (2026-08 확정 · 쿠쥬 타운하우스는 2026-09)** — 별장·독채는 **리조트 이름을 떼고 각자 이름**을 쓴다.
  본관만 리조트 이름을 쓰는 일본 리조트 구조를 따른 것이다.

  | 실체 | 이름 | 영문 |
  |---|---|---|
  | 본관 트윈 100실 | 야마나미 호텔 | YAMANAMI HOTEL |
  | 8인 별장 1동 | **아소 프레스티지** | ASO PRESTIGE |
  | 2인 별장 3동 | **히노키 스테이** | HINOKI STAY |
  | 4인 독채 1동 | **소보 테라스** | SOBO TERRACE |
  | 2인 독채 10동 | **쿠쥬 타운하우스** | KUJU TOWNHOUSE |
  | 2인 돔형 4동 | **돔스테이** | — |

  - **일본어 발음대로 적는다(2026-09-09 · Min) — 「쿠주」 → 「쿠쥬」(쿠쥬 타운하우스 · 쿠쥬 코스), 「구주고원CC」 → 「쿠쥬코겐CC」.**
    久住 = くじゅう, 高原 = こうげん(탁음 없음 → 「코겐」이지 「고겐」이 아니다). 영문 KUJU TOWNHOUSE · 사진 폴더 `kuju` · 내부 키 `kuzu` · 엠클릭 별칭
    「쿠주힐즈」·「구주힐즈」(엠클릭 표기) · `tools/library` 서류 제목 「쿠주힐즈」(원본 이름) · 레거시 사본은 그대로.
    DB 짝 = `29_kuju_spelling.sql`(mt_block_override hotel 「쿠주 타운하우스」 → 「쿠쥬 타운하우스」 · 2026-09-09 실행).
    **기준 = 지명·고유명은 발음(쿠쥬 · 쿠쥬코겐 · 벳푸), 보통명사로 된 이름은 뜻 번역(2026-09-09 · Min 확정).** 그래서 **국제공항CC(미야자키) ·
    스가다이라 고원 온천호텔 · 나고야 중부국제공항은 그대로** — 「코쿠사이쿠코CC」「스가다이라코겐」처럼 바꾸지 않고 병기도 하지 않는다. 다시 묻지 말 것.
    검사 `audit.mjs`·`about.mjs` 는 「쿠주 」·「구주고원」을 옛 표기로 잡는다.
  - 형태는 **「고유명 + 종류」 두 낱말**로 맞춘다. 낱말 수가 같아야 표에 나란히 놓았을 때 읽힌다.
  - **돔스테이만 붙여 쓴다.** 홈스테이·템플스테이·팜스테이처럼 「-스테이」 조어는 한 낱말로 굳어 있고,
    「돔」은 고유명이 아니라 형태라 「돔 스테이」로 띄우면 이름이 아니라 설명(돔에서 머묾)으로 읽힌다.
    영문 DOME STAY 는 HINOKI STAY 와 나란히 두 낱말 그대로.
  - **쿠쥬 타운하우스(2026-09 확정 · Min).** 2026-08 「쿠주 프라이빗 레지던스」 → 2026-09 초 「쿠주힐즈」를 거쳐 확정한 이름.
    「고유명 + 종류」 두 낱말이라 띄어 쓴다. 영문 KUJU TOWNHOUSE. DB 짝은 `26_kuju_hills.sql` → `28_kuju_townhouse.sql`.
    **장기숙박 회원권도 `/about/` 에서 숙소와 같은 「쿠쥬 타운하우스」로 부른다 — 「롱스테이」는 쓰지 않는다**(2026-09 · Min.
    옛 표시 이름 「쿠주힐즈 롱스테이」). `/2027/` 카드는 「장기숙박 이용권 회원님 전용」이라고만 적는다. `tools/library` 의 가입 서류
    제목(「쿠주힐즈 …」)은 서류 원본 이름이라 그대로. 엠클릭 상품명은 여전히 「쿠주힐즈」라 사내 도구 `aliases` 에
    「쿠주힐즈」를 다시 넣었다(이름과 같다고 뺐던 것).
    레거시 사본(`/reserve/book/` · `merittour-tools` `tools/booking/`)은 예전 이름 그대로 두었다 — 어디서도 링크하지 않는다.
  - **이름에 재료·사실을 박지 않는다.** 예전 「히노키빌라」가 캐빈(편백 맞음)과 패밀리(편백 아님)를
    한 브랜드로 묶어 절반이 거짓이 됐다. **편백 표기는 히노키 스테이에만** 쓴다.
  - **「별장」은 이름에 쓰지 않는다.** 다섯 곳이 전부 별장이라, 하나에만 붙이면 나머지가
    별장이 아닌 것으로 읽힌다. 분류는 설명(`lg-desc`)에서 말한다.
  - 내부 키(`sobo_villa`·`sobo5`·`kuzu`·`dome_house`·`aso_villa`)는 **바꾸지 않는다** —
    엠클릭 상품명 매칭이 「소보별장」·「통나무」·「쿠주힐즈」로 걸려 있다.
  - **사내 도구(`merittour-tools`)도 같은 이름으로 맞춰 두었다.** 리조트 마스터 `name` 은
    `mt_block_override` 의 기본키(hotel)로도 쓰여, 이름을 바꿀 때는 **저장된 웹오픈 수량
    조정을 옮기는 SQL**이 반드시 따라와야 한다(안 옮기면 조정이 옛 이름으로 떠돌아
    잔여가 조정 전 값으로 돌아간다 — 없는 방을 파는 쪽으로 틀린다).
    `supabase/migrations/20_lodging_rename.sql`(야마나미* → 새 이름)과
    `21_aso_prestige.sql`(→ 아소 프레스티지) · `22`·`23`(→ 히노키 스테이·소보 테라스) ·
    `26_kuju_hills.sql`(→ 쿠주힐즈) · `28_kuju_townhouse.sql`(→ 쿠주 타운하우스) · `29_kuju_spelling.sql`(→ 쿠쥬 타운하우스)이 그 짝이다.
- **문장은 회원 시점으로 쓴다.** 「메리트투어가 운영하는/엄선한 ~」·「저희가 ~」 대신
  「회원님께서 이용하실 수 있는 ~」처럼 회원이 주어가 되게 한다.
  예외 = `/about/`(회사소개)의 **브랜드 정의문**과 **계약 관계 설명**(총판·대리 송금 등)은 회사가 주어여야 맞다.
- 연락처: ㈜메리트투어 02-365-9800 (평일 09:00~18:00).

## 2027 회원 예약 개편 (2026-08 진행 상황)

### 목표와 방향
- **엠클릭(예약 시스템 업체)에 기존 예약 사이트 개편을 요청** 중. 메일 발송 완료, 회신 대기.
- **API 연동은 안 묻기로 함(Min)** → 엠클릭이 자기 시스템에 `/reserve/` 화면을 **이식**하는 방향.
- 핵심 요청 = **상품을 줄이는 것이 아니라 화면만 개선**. 손님에게는 숙소 단위로 하나로 보이고, 출발지·기간 선택에 따라 뒤에서 상품이 자동 매칭되게.
- 엠클릭에 보내도 되는 링크 = **`/reserve/` + `/2027/`** (실제 개인정보 없음·noindex·spec 링크 없음 확인). **`/reserve/spec/`은 보내지 않는다.**

### 엠클릭 현황 (스크린샷 확인)
- **이미 있는 것** — 룸타입·베드타입(TWN/QUAD/SGL/DBL)·**날짜별 블록수/사용수/잔여수**·룸타입별 판매가. 「N실 남음」은 이걸 읽으면 된다. 새로 만들 것 없음.
- **추가로 필요한 필드 4개** — ①룸타입 온라인 노출 on/off(야마나미 소형트윈 제외용) ②출발 가능 요일 ③온라인 판매 구분 3단계(일반/기획전/문의전용) ④예약 인원 하한·상한.
- **묶는 기준은 상품명이 아니라 「대표호텔」 코드**로 할 것. 상품명 파싱은 반드시 어긋난다(우리 `aliases` 12개·`roomAlias` 38곳이 그 증거).
- 상품 수: 현재 124건 → `/reserve/` 기준 재구성 시 80건(숙소15 × 출발지 × 박수). 출발지를 상품 옵션으로 두면 41건이나 엠클릭 구조 변경 필요 → **80건 유지 권장**.
- **2027 이름 통일 예정(Min)** — 엠클릭 호텔명·룸타입명을 우리 리조트 마스터 이름에 맞춘다. 맞추면 `aliases` 부채가 사라진다. **관내별장은 야마나미 호텔에 합치는 것으로 확정**(블록수도 합산해야 보유가 맞음). **스가다이라 고원 온천호텔은 그대로**(숙소=고원 온천호텔 / 골프장=그린CC, 서로 다른 이름).

### `/reserve/` 구현된 것
- **룸타입 선택** — 날짜 고르면 룸타입별 잔여 표시 후 선택. 1종이면 자동 선택. 마감일은 대기 신청이라 건너뜀. 야마나미=트윈 100실 단일(소형트윈 미노출), 포틴힐즈 호텔=트윈13·패밀리룸19(4인실).
- **인원 하한·상한** — `minPaxOf`/`maxPaxOf`. 독채는 동 단위라 정원이 곧 최소(소보 테라스 4명·프레스티지 8명). 회원권 인원으로 못 잡는 상품은 **버튼 비활성 + 사유 표시 + `pvGo`에서 재차단**.
- **기획전(`PROMO`)** — 일반 목록·검색에서 제외, 오른쪽 상단 「기획전」+N배지로만 진입. `aso_villa`=회원권 무관 예약 가능(8인). `kuzu`(쿠쥬 타운하우스)=**`contactOnly`** — 장기숙박계약 자격을 온라인에서 가릴 수 없어 예약 대신 문의(전화·카카오)로 보냄.
- **차수 차단** — `roundBlock()`이 「자격 없음」과 「휴지 기간」을 구분.
  **특별 회원권은 우선예약 대상이 아니다**(2026-08 변경 · 이전 규칙 「3차만 참여」는 폐기).
  어느 차수에도 배정하지 않고(`eligibleRound` 항상 false → 배정 0회), 우선예약 기간 내내 예약 진행을 막으며,
  상시예약부터 일반 고객과 같은 조건으로 예약한다. 차수를 「해당 없음」으로 늘어놓지 않고
  **「우선예약 대상 아님 · 상시예약 ○○부터」 한 줄**로 보여 준다 — 늘어놓으면 언젠가 자기 차례가 오는 것으로 읽힌다.
  같은 규칙이 `/reserve/spec/` FR-5.2 · `/reserve/book/`(레거시) · `merittour-tools` `tools/booking/`(v15)에
  함께 있다. 한쪽만 고치지 말 것. **`/2027/`(손님 공지문)에서는 특별 회원권 언급을 전부 뺐다** —
  우선예약 안내문인데 대상이 아닌 등급을 적어 두면 참여하는 것으로 읽힌다.
- **상품별 우선예약 허용 회원권(`PRIORITY_ONLY`)** — **포틴힐즈CC 호텔·카라반은 골프장과의 2027 계약에 따라
  우선예약 기간에 EWRC이용권·EWRCⅡ만 예약**할 수 있다(2026-09 변경). 상시예약부터 전 회원.
  엠클릭에는 「이벤트 상품」으로 등록해 회원권 구분으로 제한한다(협의 완료).
  **기획전(`PROMO`)에 넣지 않는다** — 넣으면 목록·검색에서 빠져 상시예약 때도 나고야로 찾을 수 없다.
  목록 카드 배지(「우선예약 EWRC 전용」) + 상세 체크 항목 안내 + `priorityBlock()`으로 진행 차단(대기 신청 포함),
  상시예약이 시작되면 셋 다 사라진다. 회원권 비교는 `gradeKey()`(띄어쓰기·「2/Ⅱ」 무시).
  같은 규칙이 `/reserve/spec/` FR-5.2a · `/2027/`(s-02 회차 안내 끝 + 나고야 블록 머리)에 있다.
  **레거시(`/reserve/book/`·`merittour-tools` `tools/booking/` v15)에는 넣지 않았다** — 어디서도 링크하지 않는 사본이다.
- **기간 이름(2026-09 확정 · Min)** — 앞 = 「**우선예약 기간**」(앞에 「회원」을 붙이지 않는다 — 회원 우선 예약제는 제도 이름이라,
  기간에만 붙이면 그 뒤에는 회원 우선이 끝나는 것으로 읽힌다), 뒤 = 「**상시예약 기간**」(옛 「자유예약」 — 「자유」가 비회원 개방으로
  읽혀 바꿨다). 둘 다 붙여 쓴다. 「추가 예약」(첫 예약인 회원에게 안 맞음)·「일반 예약」(일반 고객으로 읽힘)은 쓰지 않는다.
  **2026-09-07 재검토 끝 「상시예약 기간」 유지 확정(Min)** — 「수시예약」·「전 회원 예약」·「선착순 예약」·「우선예약 이후 예약」을 놓고 봤고,
  「상시예약 기간이 제일 낫다」로 결론. 다시 묻지 말 것.
  상시예약 기간에도 예약은 회원님 성함으로 진행된다는 한 줄을 `/2027/` s-02 와 `/reserve/` 마이페이지에 두었다.
  **손님 화면에서는 「회원님 성함」(○) / 「회원님 이름」(×)**(2026-09 · Min). spec 의 「회원 이름」은 개발 문서라 그대로.
  코드 식별자(`isFreePeriod`·`FREE_OPEN_AT`·`d.free`)는 그대로. `/reserve/book/`·`merittour-tools` `tools/booking/` 의 표기도 같이 바꿨다.
- **시연 5종** — HONG(홍길동·EWRC이용권·4인) / KIM(김영희·특별·2인·**우선예약 대상 아님**) / CHOI(최민창·EWRCⅡ·16인) / PARK(박정우·다이아1·4인) / LEE(이수진·골드·2인·전 차수). 비번=아이디 뒤 휴대폰번호. **로그인 창 = ①회원 선택 → ②차수 선택 → 바로 로그인** + ③아이디 직접 입력.
- **예약 취소** — 없으면 시연을 한 번밖에 못 한다. 취소 시 **그 건이 쓴 차수 횟수까지 복구**(`usedRound`).
- **화면 맞춤** — `fitPanel()`이 실제 높이를 재서 날짜 칸 크기 결정(sticky header 높이 차감). 예약 진행 버튼까지 한 화면에. 폰은 CTA 하단 고정. **달력 접기 토글은 불편해서 롤백**(항상 펼침).
- hover 규칙 12개는 `@media (hover:hover)`로 — 터치에서 잔상이 남아 엉뚱한 버튼에 불이 들어왔다.
- 시즌 = **2027-03 ~ 2027-12**. 우선예약 차수는 2026-10(2026 가을에 2027 출발분 예약) — **오픈 2026-10-19(월) 10:00**(2026-09 에 10-12 → 10-19 · 10-26 안도 잠시 있었으나 일단 10-19 · Min).
  1차 월~화 · 2차 수~목 · 3차 금~토. **상시예약은 3차 종료와 동시가 아니라 우선예약을 다 정리한 뒤 11월 말 오픈 예정**(2026-09 · Min · 날짜 미확정).
  `/reserve/` 의 `FREE_OPEN_AT` 은 자리값 `2026-11-30T10:00`(시뮬레이션·판정용)이고 화면에는 `FREE_OPEN_LABEL`(「11월 말(예정)」)을 보여 준다 —
  날짜가 확정되면 `FREE_OPEN_AT` 을 맞추고 `FREE_OPEN_LABEL` 을 빈 문자열로 두면 날짜가 그대로 찍힌다.
  오픈일·차수 날짜는 `/2027/` s-02 + `/reserve/` `PRIORITY_SCHEDULE`·`FREE_OPEN_AT` + spec §5.1 표 세 곳을 함께 고친다.

### `/2027/` 색 — 베이지 라이트 고정 (Min)
- **다크모드 분기 금지.** `@media (prefers-color-scheme:dark)` 를 넣지 않는다. 기기 설정에 따라 같은 안내문이
  갈려 보이고, 직원이 스크린샷을 떠 보낼 때도 사람마다 다른 그림이 나간다. `[data-theme]` 분기도 두지 않는다.
- 팔레트 = `--paper:#F6F2E8` 베이지 · 글자는 올리브/골드 계열 유지(`--ink:#1E2618` `--ink-soft:#4C573F`
  `--muted:#5F6B47` `--gold:#7F6019`) · 표 머리와 안내 박스는 `--band:#EDE7D6` · 회원 패널은 흰 카드 + 골드 테두리.
- **세이지(연한 초록)로 되돌리지 말 것** — `merittour-tools/docs/design_2027_guide_readability.md` §1 은 세이지를
  제안하지만, 바탕이 초록이면 글자(올리브·골드)와 색 계열이 같아 덜 떨어져 보인다는 판단으로 베이지로 바꿨다.
  (같은 문서의 「베이지·골드 계열 비선호」 원칙보다 이 결정이 우선한다)
- 색을 만질 때는 **글자별 대비비를 실제 렌더에서 재고** WCAG AA(본문 4.5) 미달이 0건인지 확인한다.
  현재 **최저 4.62 · 본문 13.96**(574개 요소 전수). `--muted` 는 라벨 50곳(`.facts dt`)을 좌우해 가장 먼저 티가 난다.
  - ⚠ **반투명 배경은 아래 배경과 겹친 색으로 잰다.** 안 겹치면 `.tbd`(「확정 예정」 칩)처럼 띠와 글자가
    같은 색이라 1.00 이 나오거나, 실제 4.47 을 4.62 로 잘못 읽는다. 이 때문에 칩이 한동안 AA 미달로 떠 있었다
    (띠를 12% → 8% 로 낮춰 4.70). `color(srgb 0..1)` 표기도 `rgb()` 로 착각해 읽지 말 것.

### `/2027/` 사진·영상
- 지역마다 **숙소 카드 안 사진 줄(`.shots`) + 골프장 사진 블록(`.media-open`)**을 둔다.
  파일 자리 = `2027/photos/<지역>/<숙소폴더>|golf/01.webp` 부터 **번호를 이어서**(장수 제한 없음). 규격·올리는 법은 `2027/photos/README.md`.
  **접는 블록은 두지 않는다** — 숙소도 골프장도 항상 보인다. 접어 두면 사진이 있는지조차 눌러 봐야 안다.
- **빈 자리(＋ 칸)·경로 안내줄을 화면에 두지 않는다(2026-09 · Min).** 회원에게 발송하는 **공식 홈페이지**라
  「2027/photos/… 에 01.webp 부터」 같은 직원용 안내가 보이면 안 된다. 올리는 법은 `photos/README.md` 에만.
  사진이 없는 숙소는 `.shots` 를 `hidden` 으로 두고 **첫 장을 찾을 때** 연다(`buildShots` 의 `onFirst`),
  골프장은 블록(`.media-open`)째, 영상까지 없으면 `.media` 상자째 숨긴다(빈 상자도 위아래 여백은 차지한다).
  숨긴 요소는 IntersectionObserver 에 안 걸리므로 골프장 블록은 자기가 아니라 **지역 머리(`.r-head`)**를 지켜본다
  (`whenNear` 는 한 요소에 여러 일을 걸 수 있게 목록). `.shots` 는 `display:flex` 라 UA 의 `[hidden]` 이 져서 CSS 로 다시 잠갔다.
  예전 규칙(「틀을 감추지 않는다 · 빈 칸에 폴더 이름」)은 폐기 — 그 반대로 되돌리지 말 것.
- **숙소 사진은 그 숙소 카드 안, 설명 바로 아래에 붙인다.** 접는 블록으로 빼 두면 어느 숙소 이야기인지 눈으로 다시 이어 붙여야 한다.
  카드에 `data-lg="폴더"` 만 붙이면 자리가 생기고, 이름은 `.lg-name` 에서 읽으므로 두 곳에 안 생긴다. **지역 8곳 모두 `<ul class="lodging">` 카드를 갖는다**(예전에는 구마모토만 카드였고 나머지는 `<dt>숙소</dt>` 한 줄이었다).
- **사진 줄은 가로로 민다**(`.shots` flex + `overflow-x`). 세로로 쌓으면 숙소마다 카드가 화면 하나씩 차지한다. 데스크톱은 휠로 가로 이동이 안 되니 `dragStrip` 으로 끌기를 붙였다 — 6px 문턱을 넘긴 뒤에야 끌기로 보고 뒤따르는 클릭 한 번을 삼킨다(안 삼키면 밀었을 뿐인데 크게 보기가 열린다).
- **`.lodging` 은 한 줄에 카드 하나**(`grid-template-columns:1fr`). 두 칸이었을 때는 카드 폭이 절반이라 사진 네 칸 중 둘만 보이고 나머지가 잘렸다.
- **칸 폭은 `clamp(130px, calc((100% - 24px)/4), 190px)`** — 카드 폭에 네 칸이 딱 맞는다(PC 에서 잘리는 줄 0). 폰은 네 칸을 담으면 한 칸이 70px 대로 줄어 무엇인지 안 보이므로, 두 칸을 온전히 보이고 세 번째를 걸치게 둔다(밀 수 있다는 표시).
- **`.lodging li` 에 `min-width:0`.** 그리드 칸의 기본 `min-width:auto` 가 안쪽 가로 스트립의 전체 폭을 그대로 받아 페이지가 통째로 옆으로 밀렸다(900px 화면에서 780px 넘쳤다).
- **칸 크기는 `.shots>*` 하나가 정한다.** `.shots button` 에 `width:100%` 를 또 주면 특이도가 높아 그 값이 이겨, 사진이 들어온 칸만 스트립 전체 폭으로 늘어난다.
- **카드는 접혀 있지 않으므로 접힘에 기대면 안 된다.** 사진은 그 카드가 화면에 가까워질 때(`IntersectionObserver`, 400px 여유) 만든다.
- **있는지는 HEAD 로 묻고(`exists`), 사진은 칸이 보일 때 받는다(`whenSeen` · 스트립을 뿌리로 한 IO · 가로 240px 앞서).**
  예전에는 `new Image()` 로 두드려서 그것이 곧 내려받기였다 — 카드가 가까워지면 야마나미 호텔 26장 10MB 를 다 받았다(구마모토 한 지역 48MB).
  `file:`·fetch 없음·HEAD 거절(404 이외의 실패)은 이미지로 두드린다(최악 = 예전 동작). 카드가 보일 때 데스크톱 6장·폰 4장만 받는다(2026-09).
  `<img>` 의 alt 는 비운다 — 이름은 버튼 `aria-label` 에 있고, alt 를 주면 받기 전 글자가 칸에 비친다.
- **사진은 `2027/photos/` 아래에만.** 저장소 맨 위에서 Upload 하면 루트에 `kumamoto/` 가 생긴다(2026-09 에 한 번 — 15.webp 만 제자리로 옮기고 지웠다). README 에 경고를 적어 뒀다.
- **올리기 링크(`/upload/main/2027/photos/<지역>/<숙소>`)로 열었을 때는 파일만 끌어다 놓는다.** 폴더째 끌면
  `nagoya/hotel/nagoya/hotel/` 처럼 한 단계 깊어져 안 보인다(2026-09 포틴힐즈 — 제자리로 옮겼다). README 「바로 올리는 링크」 표에 경고.
- **사진 순서 = 외관 → 객실 → 식당 → 욕실·온천 → 로비·매점·세탁 같은 시설**(2026-09 · Min). 크게 보기가 번호 순서로
  넘어가므로 올릴 때 이 순서로 번호를 매긴다. 2026-09 에 11개 숙소 130장을 이 순서로 다시 매겼다(번호 시트를 만들어
  한 장씩 보고 분류 · `git mv` 두 단계로 번호만 바꿈 — 파일 내용은 그대로).
- **장수를 미리 정해 두지 않는다.** `01` 부터 **하나씩 차례로** 두드려(`probe`) 있으면 붙이고 없으면 멈춘다. 한꺼번에 상한만큼 두드리면 99장 × 24자리가 되고 대부분 헛걸음이다. 사진이 하나도 없는 숙소는 `01` 한 번만 두드리고 끝난다.
  - **상한 `SHOT_MAX` 는 99**(파일 이름 두 자리). 24 였을 때 야마나미 호텔 26장·시즈노야도 28장의 뒤가 **소리 없이 잘렸다**(2026-09 · 「26.webp 가 왜 안 보이나」). 없는 번호에서 멈추니 값을 키워도 헛걸음은 늘지 않는다.
- **크게 보기(라이트박스)** — 사진을 누르면 **다음 사진**(한 장뿐이면 그대로), 좌우 버튼·`←` `→` 키, **바깥 어두운 곳**·`Esc` 로 닫기. 아래 줄에 「숙소 이름 · 사진 N장 중 M번째」 + 조작법 한 줄(`aria-live`). 「3 / 12」는 시니어 회원에게 분수로 읽혀 말로 바꿨다(2026-09).
  - 그래서 **번호를 건너뛰면 거기서 끊긴다**(`01`·`03` → `02` 에서 멈춰 `03` 이 안 보인다). README 에 적어 뒀다.
- **골프장도 숙소와 같이 항상 열려 있다**(`.media-open`). 접기(`<details>`)와 「지나가면 자동으로 접기」는 걷어냈다 — 접히는 것이 없으니 함께 죽은 코드가 된다.
- 크게 보기는 **그 숙소 사진 안에서만** 넘어간다. 블록 전체를 한 목록으로 묶으면 프레스티지에서 넘겼는데 료칸이 나온다.
- **접혀 있는 동안 `<img>` 를 만들지 않는다.** 만들어 두면 지역 8곳 × 블록 2개 = 96장을 문서 열 때 받는다.
  `<details>` 의 `toggle` 에서 처음 열릴 때 한 번만 만든다.
- **영상은 저장소에 넣지 않는다.** 4K 한 편이 수백 MB다. 유튜브 주소를 `VIDEO[지역] = { url, label }` 에 적으면
  골프장 사진 블록 **위**에 「🎬 label」 블록이 생기고, **썸네일을 누를 때만** 같은 16:9 틀 안에 플레이어(youtube-nocookie)를 끼운다 —
  처음부터 박으면 지역마다 유튜브 스크립트가 딸려 온다. 버튼을 iframe 으로 통째로 바꾸면 틀이 사라져 300×150 으로 찌그러진다.
  2026-09 구마모토(아소 야마나미 리조트 4K) 등록, 포틴힐즈는 주소 대기.
- 라이트박스는 닫을 때 **누른 자리로 초점을 돌려주고** `body` 스크롤 잠금을 푼다. 안 그러면 뒤 문서가 같이 움직인다.

### `/2027/` 문안 결정 (Min)
- **회원 안내만** — 「일반 고객」 언급 0건. 현지 상황 원칙 제거.
  - **등급별 예약 가능 인원 블록은 되살렸다**(2026-08). 한 번 뺐다가 다시 넣은 자리이니 또 빼지 말 것.
    숫자의 짝은 `/reserve/` 의 `DEMO_ACCOUNTS` 다 — 다이아1·다이아2·EWRC이용권·EWRCⅡ 4인 / 골드 2인.
    **특별 등급은 이 표에 넣지 않는다**(우선예약 대상이 아니다). 특별 회원권 자체는 남아 있으므로
    사내 도구(`tools/dashboard` 인원 기본값 · `tools/library` 회원권 자료)에서는 그대로 둔다.
    한쪽만 고치면 같은 회원권이 두 화면에서 다른 인원으로 보인다.
  - **「모든 회원님께 고르게 자리가 돌아가도록」 블록(인원 초과 취소 · 겹치는 날짜 중복 예약 · 최대 연속 체류일 초과)은 2026-09-09 삭제(Min).**
    인원 초과와 중복 예약은 예약 시스템에서 애초에 막히니 적을 것이 없고, 연속 체류일은 06 유의사항에 이미 있다. 되살리지 말 것
    (예전 메모 「인원 초과 취소는 표와 한 몸」은 폐기).
  - **리노베이션·보수 문장은 히어로가 아니라 구마모토 블록 머리의 `.callout`(「2027 시설 안내」)에 둔다(2026-09-09 · Min — 「전체 안내에 야마나미만 나오니
    야마나미 안내에」).** 히어로 리드는 전 지역 공통 인사만. 골프장 사진·영상 블록은 JS 가 `.callout` 아래에 끼우므로 안내가 사진 위에 온다(나고야와 같은 구조).
  - **02 로그인 방법에 예시 한 줄(홍길동 · 010-1234-5678 → HONG01012345678 / 01012345678)과 표기 안내**(영문 성은 여권 철자 대문자 ·
    휴대폰번호는 하이픈 없이 · 메리트투어에 등록된 번호 기준 · 번호가 바뀌면 카카오톡 채널) — 2026-09-09. 예시는 `/reserve/` 시연 계정과 같은 가상 번호.
- **포함 서비스 미이용 고지(2026-09-09 · Min)** — 06 유의사항에 「회원님 사정으로 포함 서비스(송영·식사·라운딩 등)를 이용하지 않으셔도(렌터카 개별 이동 등)
  그 부분의 요금은 제외·환불되지 않으며, 일정 밖 이동은 회원님 책임 · 개별 일정은 예약 전 상의」. `/reserve/` 상품 상세 하단(`pvFoot`)·spec FR-6.17 도 같은 문구.
  **「회원님 사정으로」를 꼭 붙인다** — 우리 쪽 사유(결항·차량 미운행)까지 환불 불가로 읽히면 불공정 조항 소지. 금지가 아니라 「제외되지 않는다」로만 말하고,
  항공권 개별 구매는 계약서 제7조(회원관리비는 그대로)와 맞물리니 따로 막지 않는다. 원칙은 계약서 제5조 7항(체재비 부분 제외·환불 불가)과 같다.
- **04 환율 문구 강화(2026-09-09 · 민원 사례 반영 · Min)** — 「환율이 오르거나 내리더라도 추가 청구도 차액 정산도 없다」(종전 「추가 정산 없음」은 상승 쪽만 말한 것으로 읽혔다) ·
  「안내문의 엔화 금액은 원화 산정 기준 금액 · 엔화 직접 송금·현지 지불은 운영하지 않는다」 · 「체재비·송영비는 시즌마다 새로 정해져 안내된다」. spec FR-6.11 도 같이.
  사례는 `merittour-tools/docs/cases/2026-09_membership_fx_dispute.md`(익명).
- **06 「안내하지 않은 일정의 비회원 투어 = 비회원가」(2026-09-09 · Min)** — 안내문에 없는 일정을 회원이 참여하지 않고 가족·지인만 가는 투어로 요청하면 비회원 투어로 진행되어
  비회원가 적용. 회원가는 회원이 직접 참여하는 예약에만(계약서 제8조 5항과 같은 원칙). 2026-09-07 에 뺀 「비회원 투어 전환」 문장(회원이 부득이하게 못 가는 경우)과는
  다른 상황이라 되살린 것이 아니다. 검사 `audit.mjs` 의 「비회원」 금지 항목은 이때 뺐다.
- 「회원제」 → **「회원 우선 예약제」**. h2 = **「회원님의 한 해를 준비합니다」**(우선·먼저 배정 같은 순위 표현 배제).
- s-01 리드 = **「골프 상품은 모두 회원 우선 예약제로 운영됩니다」**. 「메리트투어 골프 상품은 ~」처럼 회사를 앞세우지 않는다.
- 원칙 3 = **「회원님 예약이 한 해 운영의 기준입니다」**(「미리 예약하시는 것이 유리합니다」로 바꿨다가 되돌림). 근거는 코스 관리만이 아니라 **연간 항공 좌석 확보**를 함께.
- **리조트별 최대 연속 체류일** — 회원권 연결·예약 이어붙이기로 장박 만드는 것 차단. 하루라도 비면 별개. 스가다이라만 14박이라 리조트별 값이 다름. 판정 규칙은 `spec/` §6.0.
- **요금 표기 = 「무엇이 포함된 금액인가」**. 「패키지」·「원화 총액」은 쓰지 않는다(전 상품이 패키지고, 손님은 어차피 원화로 받는다).
  본문에서는 「투어 요금」으로 쓴다(아키바 「석식은 투어 요금에 포함되지 않아」 · 2026-09-07 정리). 제목 「2027 시즌 골프 패키지 예약 안내」의 「패키지」는 상품 종류를 뜻하는 말이라 그대로.
- **04 요금·환율 기준의 범위 = 체재비·송영비를 1박 단가로 안내한 상품**(2026-09-07 · Min — 「전 지역 공통이라지만 총 패키지가로 묶은 것은 다르다」).
  항공·숙박·골프를 **하나의 1인 금액으로 정한 상품**(지금은 무츠키 료칸 · 그랜드 머큐어 · 스가다이라 고원 온천호텔)은 안내된 원화 금액이 기준이고 환율 산정 규칙을 적용하지 않는다 —
  04 머리말에 적고 spec FR-6.11 에도 한 줄. **04 에는 상품 이름을 늘어놓지 않는다**(Min · 「다른 곳이 추가로 생길 수도 있다」 — 이름은 spec 에만 예시로).
  첫 항목은 「체재비 1인·1박 / 송영비 1인·왕복」으로 나눠 적었다(「모든 금액은 1인/1박」은 송영비에 틀린 말이었다).
- **02 접수 방법 아래 「로그인 방법」**(2026-09-07 · Min · 기존 로그인 안내 그대로) — 회원가입 없이 아이디 = 대문자 영문 성 + 휴대폰번호(예 HONG01012345678),
  비밀번호 = 휴대폰번호. 로그인 후 비밀번호 변경 권장 · 안 되면 카카오톡 채널 문의. 예시 아이디는 `/reserve/` 시연 계정과 같은 가상 번호.
  접수 방법에는 **「예약 홈페이지는 우선예약 기간 오픈 전에 전면 개편 예정」** 안내 한 줄(`.note`) — 엠클릭 이식이 끝나면 새 화면 이용 방법을 다시 안내한다.
- **공유 전 최종 점검(2026-09-07)** — `scratchpad` 의 `audit.mjs` 같은 검사(금지 표기·확정 예정 칩·앵커·JS 오류·가로 넘침·사진·메타)를 돌리고 본문 전문을 읽는다.
  - 02 회차 표에는 **요일만이 아니라 날짜**를 적는다(「10월 19일(월) 오전 10시 ~ 20일(화) 오전 10시」) — 요일만 있으면 「2차가 언제냐」를 직원이 계산해야 한다.
  - **카카오톡·문자 링크 미리보기**: `<head>` 에 `og:title`·`og:description`·`og:image`(`2027/og.jpg` · `photos/kumamoto/golf/05.webp`(아소산·야마나미 호텔·코스)를 1200×630 으로 자른 JPEG — 미리보기는 webp 를 못 읽는 앱이 있다). 사진을 바꾸면 og.jpg 도 다시 만든다(`scratchpad/og.mjs`).
  - 「골프장 내 숙소」 → **「골프장 안 숙소」**(시로사토 카드 · `/reserve/` meta · photos README) — 「내」는 공문 투(「리조트 안 독채 숙소」와 같은 이유).
  - 성수기 안내의 「달력에 표시된 공휴일」은 이 페이지에 달력이 없어 「일본 공휴일 외에도」로 바꿨다.
  - **줄바꿈(2026-09-07)** — `word-break:keep-all` 이라 낱말 사이에서만 끊기는데, 폰(360~430px)에서 「(예:」와 아이디 · 「오전」과 「10시」 · 「약」과 시간 ·
    「1인」「1박」과 금액 · 「2인」과 「1실」 · 요일 「→」 요일 · 「18홀 + 9홀」 같은 짝이 줄 끝에서 갈라졌다. 그 사이는 `&nbsp;` 로 묶는다(약 100곳).
    가운뎃점(·)이 줄 끝에 오는 것은 목록 구분 표기라 그대로 둔다(줄 머리로 보내면 `.cav` 의 글머리 「·」와 헷갈린다).
    검사는 `scratchpad/wrap.mjs`(너비 3종에서 낱말별 사각형으로 줄을 나눠 짝이 갈라진 곳·고아 줄을 찾는다).
    **테스트 스크립트는 `innerText` 의 ` ` 를 보통 공백으로 바꿔 비교한다** — 안 바꾸면 「오전 10시」 같은 문자열 검사가 조용히 실패한다.
- 주류 = **일본 쇼츄(焼酎)** · 프레스티지 = **시로(쌀) · 쿠로키리시마(고구마) 중 1병 서비스**(2026-09 · Min · 종전 「2병」 · 재료 표기는 2026-09-07) + **전용 가라오케 무료 제공**.
  **아소 프레스티지 체재비 1박 18,000엔은 2026-09-07 확정**(Min · 「확정 예정」 칩 제거). `/reserve/` `aso_villa` `t:18000` 과 같은 값.
  「현지 사정에 따라 다른 종류의 주류로 제공될 수 있다」는 표의 `.cav` 줄에 둔다. 「일본 소주」는 한국 소주를 떠올리게 해서 안 씀. 박수 기준 아님.
  **소보 테라스는 프레스티지와 묶지 않는다**(2026-09 · Min) — 체재비 **15,000엔**(히노키 스테이·돔스테이·쿠쥬 타운하우스와 같은 줄),
  쇼츄·라운딩 전기카트 무료 혜택 없음. 카드의 「이동용 카트 제공」은 그대로. `/reserve/` `sobo5` 의 t·태그·체크 항목도 같이 바꿨다.
- 전기카트 = 아소 야마나미에서 **유료 사전 신청으로 이용 가능**(제한 표현 배제). 프레스티지 무료, 그 밖 1일 1대 2,000엔.
- **아소 프레스티지 카드 문안(2026-09-09 · Min)** = 「아소 코스 속, 오붓하게 소중한 분들과 함께 머무는 8인 별장 · 1동 · 야마나미 호텔에서 250m · 본관 이동용 카트 제공」.
  「아소산 속」→「아소 코스 속」(돔스테이 카드의 「아소 코스 옆」과 같은 띄어쓰기), 카드에서 「전용 가라오케」는 뺐다 — 체재비 표 비고와 `/reserve/` 체크 항목의 가라오케 혜택은 그대로.
  같은 날 **히노키 스테이 = 「… 야마나미 호텔에서 100m · 호텔 이동용 카트 제공」, 소보 테라스 = 「… 야마나미 호텔에서 50m」(이동용 카트 문구 없음 · `/reserve/` `CHECKS.sobo5` 도 같이)**.
  **카트 표기는 세 카드(프레스티지 · 히노키 스테이 · 돔스테이) 모두 「호텔 이동용 카트 제공」**(2026-09-09 · Min — 「본관 이동용」·「숙소 이동용」도 놓고 봤으나
  거리 표기 「야마나미 호텔에서 ○m」와 짝이 맞는 「호텔 이동용」으로). 소보 테라스는 카트 문구 없음. 쿠쥬 타운하우스는 「차로 2분 · **호텔 이동 송영 서비스 제공**」(2026-09-09 · Min — 담당자 제안 「본관이동 송영」은 「본관」이 낯설어 「호텔 이동」으로).
- 간지호텔 = **호텔 온천 + 근처 아카가와 온천도 이용 가능(입욕료 별도)**. 「100%」·목장 언급 삭제.
  **쿠쥬코겐CC 라운딩은 골프장 사정에 따라 아소 야마나미CC로 바뀔 수 있다는 안내(2026-09-09 · Min · 원문 「구주고원 골프장 사정에 따라 '아소야마나미 골프장'으로
  변경될 수도 있습니다 ※사전 안내를 드립니다만, 예고 없이 변경될 수도 있음」)** — 구마모토 라운딩 표 아래 `.cav` + 간지호텔 카드 `.soft` + `/reserve/` `CHECKS.kanji` 세 곳.
- 스가다이라 온천 24시간·여탕 시간 제한 삭제(운영시간은 바뀌면 안내가 틀린 말이 된다).
- 닐라이스프링스 = **우선예약 대상 아님 · 상시예약 기간 오픈**(블록 머리에 표기). `/reserve/`에는 없는 게 맞다.
- **포틴힐즈CC = 우선예약 기간에는 EWRC이용권·EWRCⅡ 회원만**(골프장과의 2027 계약 조건 · 2026-09). 그 밖은 상시예약부터.
  s-02 회차 안내 끝과 나고야 블록 머리 두 곳에, 「~회원님께서 예약하실 수 있습니다」 긍정문으로 적는다.
  **두 곳 다 `.callout`(골드 왼쪽 띠 상자)** — 다른 등급 회원도 놓치면 안 된다(Min). `.soft ※` 줄로 되돌리지 말 것.
  나고야는 안내가 골프장 사진 위에 오도록 JS 가 `.callout` 다음에 사진 블록을 끼운다.
- **나가노 스가다이라 · 쿠알라룸푸르 닐라이스프링스 · 벳푸 무츠키 료칸 = 우선예약 기간 접수 없음 · 상시예약 기간 오픈 예정**(2026-09 · 실제 2027 우선예약 때도 안 연다.
  무츠키 료칸은 2026-09-07 · Min — 「우선예약 말고 상시예약 기간의 예약으로 진행」).
  03 맨 아래 「상시예약 기간 오픈 예정」 `.later` 목록에 이름만(사진·요금·`.region` 없음 → 사진 자리도 안 생긴다). s-02 callout 에도 한 줄.
  레일·바로가기 목차는 **문서 순서대로**(무츠키를 `.later` 로 옮기면서 목차에서도 미야자키 뒤로 내렸다 — 레일의 「지금 보는 곳」 판정이
  목록 순서로 마지막 통과 항목을 고르므로 문서 순서와 어긋나면 엉뚱한 칩에 불이 들어온다).
  무츠키 료칸 블록(투어 요금 3박 1,450,000 · 4박 1,660,000 「2027 확정 예정」 · 사진 9장)은 git 이력에 있고 사진은 `2027/photos/mutsuki/ryokan/` 에 그대로 —
  상시예약 오픈 때 블록을 되살리면 다시 보인다.
  `/reserve/` 스가다이라·무츠키·그랜드 머큐어는 `PRIORITY_ONLY` `grades:[]` 로 전원 차단(배지 「상시예약 오픈 예정」 · `block`/`note` 문구 별도). 닐라이는 `/reserve/` 에 없다.
  `/about/` 무츠키·그랜드 머큐어 카드에 「📌 2027」 안내줄(`.region-note-small`) 한 줄.
- **그랜드 머큐어 벳푸 베이 리조트 & 스파 = 2027 신규 · 상시예약 기간 오픈 예정**(2026-09-07 · Min · 「무츠키 료칸 상품처럼」). 전단 기준:
  4박 5일(일→목) · 1인 166만 원부터 · 인천·김해·대구 출발 · 벳푸CC 3회(유후 2 · 츠루미 1) + 오기야마CC 1회(18홀 × 4회 · 카트비 포함) ·
  숙박 2인 1실 + 입욕료 · 조·석식 프리미엄 뷔페(석식 노미호다이) · 벳푸만 전망 라운지 · **중식·개인 경비 불포함**.
  `/2027/` 은 `.later` 목록에 이름만(`r-mercure` · 레일·바로가기는 무츠키 다음), `/reserve/` 는 `mercure` 상품(`pkg:{4:1660000}` · 중식 불포함은 `noLun`) —
  **보유 객실 수는 미정이라 `rm:10` 자리값**(확정되면 fee·ROOMS·spec 「미정」을 같이 고친다). 썸네일은 `reserve/assets/rooms/mercure.webp` 자리.
- **시로사토CC · 후쿠로다노타키CC 는 9홀 서비스 라운딩이 없다**(2026-09 · Min) — 9홀 추가는 현지에서 추가 비용. `/2027/`·`/reserve/`(`ROUND`·태그·venue)·`/about/` 에서
  「18홀 + 9홀 서비스」를 빼고 「9홀 추가 시 현지에서 추가 비용」 한 줄을 두었다. 야마나미·포틴힐즈·미야자키·스가다이라의 9홀 서비스는 그대로.
- **후쿠로다노타키CC 2027 재개(2026-09)** — 시로사토 아래 `r-fukuroda`(사진 폴더 `fukuroda/lodge|golf`). **체재비 1박 16,000엔 · 주중·주말 및 일본 공휴일 모두 18홀은
  확정**(2026-09-07 · Min — 시로사토CC 도 같은 값·같은 운영). **식사는 일단 시로사토와 동일**(조식 뷔페 · 중식 메인+뷔페 · 석식 요일별 세트메뉴 · 2026-09-07 · Min).
  **나리타 공항에서 골프장까지 약 2시간 30분 · 왕복 송영비 1인 12,000엔**(2026-09-07 · Min · `/reserve/` `MOVE.fukuroda`·venue 부제·fee 도 같이).
  이로써 후쿠로다 블록의 「확정 예정」 칩은 전부 없어졌다(`/2027/` 전체에 `.tbd` 0개).
  `/reserve/` `fukuroda` 상품(7박 월 출발 · 인천·김해 · 트윈 20 · 송영비 12,000)은 사내 예약 도구 v15 값 — 확정되면 RES·MOVE·CHECKS 갱신.
  사내 도구 리조트 마스터의 시로사토·후쿠로다 `stay` 기본값도 16,000 으로 맞췄다(`FIELD_BEFORE_FIX` 로 옛 기본값 0 인 저장본만 올라간다).
- **시즈노야도 거리 = 「아소 야마나미 리조트에서 차로 30분」**(구로카와 온천 기준 표기는 폐기 · 2026-09).
- **독채 숙소 카드에 야마나미 호텔에서의 거리와 이동용 카트**(2026-09 · Min) — 아소 프레스티지 250m · 히노키 스테이 100m · 소보 테라스 50m(셋 다 2026-09-09 · 종전 200m·70m·70m) · 돔스테이 200m,
  프레스티지·히노키 스테이·돔스테이는 「호텔 이동용 카트 제공」(소보 테라스는 없음 · 2026-09-09). 쿠쥬 타운하우스는 쿠쥬 코스 쪽(외부 도로)이라 **「차로 2분 · 호텔 이동 송영 서비스 제공」**(직원이 직접 송영).
  라운딩용 전기카트(프레스티지 무료 · 그 밖 1일 2,000엔)와는 다른 것이라 「이동용」을 붙인다.
- **다섯 독채(프레스티지·히노키 스테이·소보 테라스·돔스테이·쿠쥬 타운하우스)의 묶음 이름 = 「리조트 안 독채 숙소」**, 체류 일정 묶음은 「독채 숙소 · 간지호텔 · 시즈노야도 료칸」(2026-09 · Min).
  예전 「리조트 내 별장」은 돔스테이(돔형 숙소)·쿠쥬 타운하우스(레지던스형)와 어긋났고 「내」는 공문 투였다. 각 카드의 「8인 별장」「4인 독채 별장」은 그 숙소에 맞는 말이라 그대로.
  s-02 에 **상시예약 기간 정의 한 줄**(「우선예약 정리 뒤 11월 말에 열릴 예정 … 횟수 제한 없이」)도 이때 넣었다 — 닐라이·포틴힐즈 안내가
  이 낱말을 쓰는데 정의가 없었다. 상시예약에서 인원 추가·지인 동반이 어떻게 되는지는 **안내문에 적지 않는다**(규정 미확정).
- 시즈노야도 「약 700년 만간지 천연온천」 = **사실 확인 완료**(남오구니초 공식·구마모토현 관광 자료).
- 포틴힐즈 「식사 업그레이드·추가」 블록 제거 — 상추 500엔까지 있어 예약 안내에 과했다. 확정서·출발 전 안내로.
- **포틴힐즈 체류일 중식 단품 요일 = 목 · 일**(2026-09-07 · Min). 단품은 **출·귀국일에 진행**하는 것이라 2027 체류 패턴(목→일 · 일→목 · 일→일)에
  맞춰 화가 빠졌다(2026 은 화 7박이 있어 화·목·일). `/2027/` 나고야 식사 표 · `/reserve/` 포틴힐즈 체크 항목·일정표(`lunchMid`) ·
  `/guide/14hills/`(랜딩 NEW 상자 · dining 변경 안내·주간 표·범례·사진 캡션) 다섯 자리가 한 몸. 사내 확정서 마스터(2026 템플릿)의 「화, 목, 일」은
  그대로 두고 tools CLAUDE.md 의 「2027 확정서 때 반영」 목록에 적었다. 14hills dining 의 「인천·부산 동일」도 이때 「인천·김해」로 맞췄다.
- **미야자키 2027 = 골프장 2곳 · 호텔 2곳 확정(2026-09-08 · Min · 엠클릭 상품 화면 기준).** 일 도착(오후 출발 · 저녁 도착 → 크로스몰 개별 석식 · 1인 1,500엔 식비 지급)
  → 국제공항CC 호텔 2박(월 18홀 + 9홀 서비스 · 화 18홀 뒤 시내로 이동) → 미야자키 시내 호텔 3박(수·목 레인보우CC 18홀 + 9홀 서비스) → 금 니토리몰 경유 귀국.
  식사 = 조식 5(매일 호텔식) · 중식 2(수·목 클럽식 · 월·화·금 불포함) · 석식 2(월 호텔식 · 화 특식 · 일·수·목 불포함). 단독 사용 1박 25,000원 ·
  2인 플레이 시 주말·일본 공휴일에만 2백 차지(국제공항CC 20,000원 · 레인보우CC 10,000원 — 화면 표기가 원화라 원화 그대로).
  **체재비 17,000엔 · 송영비 5,000엔은 종전 값 그대로**(화면에 없었다 — 바뀌면 `/2027/` costline · `/reserve/` RES 를 같이). **항공 시각은 적지 않는다**(계절 스케줄).
  `/2027/`(일정 표 · 식사 표 · 숙소 카드 2개) · `/reserve/`(CAT · TAGS · ROUND · CHECKS · `itinMiyazaki` 전용 일정표) · `/about/`(카드 · 🏡 숙소 안내줄) · spec §2 네 곳이 한 몸.
  사진 폴더 = `miyazaki/hotel`(국제공항CC 호텔) · `miyazaki/city`(시내 호텔) · `miyazaki/golf`(두 골프장 함께). 옛 `lodge` 폴더는 사진이 없어 이름만 바꿨다.

### 남은 일
1. **엠클릭 회신 대기** — 이식 방향.
2. **`/2027/` 미확정 — 없음**(2026-09-07 · 후쿠로다노타키 체재비 16,000 · 송영비 12,000 · 라운딩 · 이동 시간까지 확정, 식사는 일단 시로사토와 동일).
   예약 변경·취소 규정은 2026-09-07 확정 — **국외여행 표준약관**을 따르되 **항공권·일부 리조트는 항공사·리조트 규정 우선**(06 절 한 줄 · 칩 제거 · Min).
   전직원 공유 전 최종 점검(2026-09-07)의 확인 항목은 모두 결정됨(Min):
   - **2027년 일본 공휴일 표를 안내문 안에 둔다**(03 끝 `#holidays` · 内閣府 「国民の祝日」 · 춘분 3/21(일)→22 대체휴일 · 추분 9/23(목) · 이용 대상 기간 3~12월만).
     「홈페이지 공지사항의 일본 공휴일 안내 참고」 문장은 뺐다(그런 공지가 없다). **해마다 표를 갈아 끼운다.**
   - **카카오톡 채널 = `https://pf.kakao.com/_dxhWus/chat`** — 떠 있는 「카카오톡 상담」 버튼(`KAKAO_URL`)과 07 문의의 링크. `/about/` 도 같은 주소.
   - **확정서 발송 = 출발 2개월 전부터**(환율도 2개월 전 확정). spec §6.3.1 도 같은 값.
   - **06 「비회원 투어 전환」 문장 삭제** — 「늘 있는 일이 아니니 너무 막지 말자」. 회원이 부득이하게 못 가는 경우는 개별 응대.
   (스가다이라·닐라이스프링스·무츠키 료칸은 상시예약 기간 오픈 예정으로 빼 두어 지금은 미확정 항목이 아니다 — 무츠키 요금은 블록을 되살릴 때 확정한다.)
3. 2027 지역 사진 — 올리지 않기로 함(없으면 스트립이 자동으로 사라짐).
4. `merittour-tools` Supabase 마이그레이션 04~18 · 20~25 **적용 완료**(2026-09 검수 — 08~11 도 저장소에 있다).
   프로젝트에 `schema_migrations` 표가 없어 `list_migrations` 는 비어 있다 — 저장소의 SQL 파일이 이력이다(`apply_migration` 쓰지 말 것).
   19(현지 전달 메모 · PR #136)만 미적용·미머지. 24(함수 EXECUTE 잠금 · notice_sent anon 회수)·25(RLS auth.uid() initplan)는 2026-09-04 실행.
5. 환율 기준일 = **출발월 두 달 전 1일(2026 · 2027 동일)**. 2027 부터 세 달 전으로 두는 안을 2026-09-07 에 Min 이 되돌렸다(「2026년과 동일」).
   `/2027/` 04(예: 6월 출발 → 4월 1일 · 확정서는 「환율이 확정되는 출발 2개월 전부터 발송」 — 2026-09-07 · Min · 「출발 2개월 전부터로 하자. 환율도 2개월 전 확정」) ·
   `/reserve/spec/` FR-6.11·§6.3.1 · 사내 대시보드 환율 카드·CLAUDE.md 를 함께 고쳤다.
   **한쪽만 고치지 말 것.** 레거시(`/reserve/book/`)는 「세 달 전」 그대로 — 어디서도 링크하지 않는다.
