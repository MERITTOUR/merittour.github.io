# merittour.github.io — 프로젝트 컨텍스트

메리트투어(MERITTOUR) GitHub Pages 사이트입니다. 정적 HTML/CSS로 구성되며,
`main` 브랜치 기준으로 GitHub Pages에 배포됩니다.

## 작업 규칙

### 표준 워크플로우 (중요)
모든 변경은 아래 순서를 별도 확인 없이 끝까지 진행한다.

1. **작업 브랜치 생성** — `main` 최신에서 작업용 브랜치(예: `claude/*`)를 만든다(또는 동기화).
2. **검증** — 변경 후 정적 검증으로 깨진 곳이 없는지 확인한다: `<div>` 등 태그 균형, 사용 클래스의 CSS 누락 여부, 링크/경로, base64·콘텐츠 보존, 가능하면 렌더 확인.
3. **커밋·푸시** — 명확한 한국어 메시지로 커밋하고 작업 브랜치에 푸시한다.
4. **PR 생성** — base `main` 으로 PR을 생성한다.
5. **Squash 머지** — PR을 **squash 방식**으로 머지한다(커밋 1개로 정리).
6. **결과 보고** — PR/머지 링크와 반영 URL을 보고하고, **필요 시 되돌리는 법(revert)** 도 함께 안내한다.

- 예외: 사용자가 "PR만", "머지하지 마"라고 하면 5번에서 멈춘다.

### 커밋
- committer: `Claude <noreply@anthropic.com>`
- 커밋 메시지는 한국어로 명확하게 작성한다.

## 사이트 구조 (주요)

```
/                          (임시) 회사소개로 리다이렉트 — 게이트 디자인은 git 이력에 보존
/about/                    회사소개 (루트 고정 주소 — 이동 금지)
/guide/                    리조트 안내 허브 (야마나미·포틴힐즈 목록)
  index.html               허브 — 리조트 선택
  yamanami/                아소 야마나미 리조트 안내
    index.html             랜딩 — 안내 메뉴 바로가기
    assets/yamanami.css    공유 다크 테마 스타일시트 (랜딩·식사 공통)
    assets/photos/         메뉴 사진 (추가·안주 사진, 주간 저녁 포스터 등)
    dining/index.html      식사 안내 — 조식·중식·석식 순서
  14hills/                 나고야 포틴힐즈 CC 안내 (랜딩 + dining)
/2027/                     2027 시즌 골프 패키지 예약 안내 (손님 공지용 · 사내 도구함에서 이전)
  index.html               전 지역 상품·요금·예약 절차 · 상단 목차 칩(모바일)·카카오톡 상담·맨 위로
  photos/                  숙소·골프장 사진 (<지역>/<숙소폴더>|golf/01.webp 부터 번호를 이어서 · 장수 제한 없음)
                           ※ 원본은 merittour-tools 에 있었고, 구 주소는 리다이렉트만 남김
/reserve/                  회원 간편 예약 (프로토타입 · Supabase 연결 전 단계)
  index.html               랜딩 = 검색 4단계(어디서/어디로/얼마나/몇 명) + 지역별 상품 목록
                           + 같은 화면 상품 상세(좌 설명·일정표 / 우 예약 가능 달력) + 로그인·마이페이지·예약확인·고객센터
                           ※ 단일 HTML, 목 데이터 인라인(CAT·RES·TAGS·CHECKS·ROUND·DEMO_ACCOUNTS·DEMO_BOOKINGS)
                           ※ 우선예약 차수(PRIORITY_SCHEDULE) 누적·차감, `?sim=ISO` 로 시각 시뮬레이션
                           ※ 상품 데이터의 단일 출처 = 사내 「2027 시즌 골프투어 예약 안내」 가이드
                             (merittour-tools `tools/booking/2027guide/`) + 리조트 마스터 보유 객실
  spec/index.html          요구사항 정의서 (개발 위탁사 전달용 · A4 인쇄 CSS · noindex)
  book/index.html          구 다단계 예약 플로우 (레거시 · 어디에서도 링크하지 않음)
  confirm/index.html       모바일 확정서 예시 (섹션 자동 접힘·동일 일정 병합·결제 현황)
/assets/fresh.js           열어 둔 탭이 옛 안내문을 들고 있는 것을 막는 검사(손님 페이지 전부에 삽입)
```
- **새 페이지를 만들면 `</body>` 앞에 `<script defer src="/assets/fresh.js"></script>` 를 넣는다.**
  탭을 며칠 열어 두면 문서를 다시 요청하는 일이 없어 캐시 설정으로는 못 고친다(실제로 `/2027/`
  숙소 카드가 두 칸이던 시절 화면이 그대로 보였다). 돌아왔을 때 `HEAD` 로 ETag 만 물어보고,
  달라졌으면 띠를 띄운다. **저절로 새로고침하지 않는다** — 읽던 자리·고른 날짜가 날아간다.
  - 새로고침하면 손해인 화면이 있으면 그 페이지에서 `window.MT_FRESH_BUSY = () => boolean` 을
    정의한다(예: `/reserve/` 는 상품·시트·완료 화면, `/2027/` 은 사진 크게 보기 중).
  - `/reserve/spec/`·`/reserve/book/`·루트 리다이렉트에는 넣지 않는다.
- **`/reserve/`는 별도 디자인 축**: 회원 예약 프로토타입이라 야마나미 다크 골드 테마가 아니라 프로토타입 고유 팔레트(시니어 친화 고대비)를 그대로 유지한다. Supabase 연결 시 목 데이터를 실데이터로 교체 예정.
- **`/reserve/`에 실제 회원 개인정보(이름·휴대폰·생년월일·고객ID)를 넣지 않는다.** public 저장소이므로 시연은 `DEMO_ACCOUNTS`(HONG/KIM/CHOI + 가상 번호)로만 한다.
- **상품 구성을 바꿀 때는 `index.html`(CAT·RES·TAGS·CHECKS·ROUND)과 `spec/index.html`(§2 상품 표)을 함께 고친다.** 2027 시즌 미진행 상품(후쿠로다노타키CC·닐라이스프링스CC 호텔·스가다이라 맨션)은 제외된 상태다.
- 계층: **(임시 게이트)`/` → 리조트 안내(`/guide/`) → 각 리조트(`/guide/yamanami/`, `/guide/14hills/`)**.
- `/about/`(회사소개)은 **루트 고정 주소**라 `/guide/` 아래로 옮기지 않는다.
- 식사 안내는 **점심·저녁 분리 없이 한 페이지**(`dining/`)에서 **조식 → 중식 → 석식** 순서로 안내한다.
- 모든 하위 페이지 좌측 하단에 플로팅 **뒤로가기 버튼**(`.top-back`, 상위 폴더로 이동).

## 야마나미 디자인 시스템 (다크 골드/포레스트 톤)

- 공통 스타일은 `guide/yamanami/assets/yamanami.css` 한 곳에서 관리한다. 페이지별 인라인 `<style>`을 늘리지 말 것. (14hills 랜딩도 이 파일을 참조)
- 컬러: 딥 포레스트 그린(`#0d1f15`/`#142c1f`) + 골드(`#C9A876`/`#E0C896`) + 와인레드 강조(`#8B3A2F`/`#C45A4A`) + 크림 텍스트(`#F2EBD5`)
- 폰트: Noto Serif JP / Noto Serif KR / Pretendard
- 페이지 컨테이너 `.wrap` 폭 760px, 골드 인셋 보더(`.wrap::before`)

## 야마나미 콘텐츠 메모

- **조식·중식**: 모두 **매일 뷔페식**(한식 위주). 면식(화·금) 운영은 **폐지**됨.
- **도착일 점심**: 인천 출발팀 = 카츠카레(라운딩 직행), 김해 출발팀 = 저녁 식사부터 일정 시작.
  ※ 손님에게 보이는 출발지 표기는 **공항 이름(인천 · 김해 · 대구)으로 통일**. 「서울 출발팀」·「부산 출발팀」은 쓰지 않는다.
  코드 내부 키(`ORIGINS[].id`, 상품 `dep:[...]`)는 `'서울'`/`'부산'`/`'대구'` 그대로 — 바꾸면 상품 매칭이 어긋난다.
- **석식(주간 저녁)**: 일 해물전골 / 월 일본산 삼겹살 / 화 매운탕 / 수 소불고기 코스 / 목 해물순두부찌개 / 금 일본산 삼겹살 / 토 흑돼지 샤브샤브. (요일별 HTML 표 `.dinner-list` + 요리 사진으로 안내. 사진은 포스터에서 추출해 `assets/photos/dinner/{sun..sat}.webp`로 보관. 원본 포스터 `weekly_dinner.webp`도 보존)
- 표기: 메리트투어 / MERITTOUR, 야마나미, 라운딩(○)/라운드(×), 바베큐(○)/바비큐(×).
- **골프장 정식 명칭 = 「아소 야마나미CC」**. 「야마나미CC」 단독 표기는 쓰지 않는다.
  리조트 이름(**아소 야마나미 리조트**)과 본관(**야마나미 호텔**)은 그대로 — 골프장이 아니다.
- **야마나미 리조트 안 숙소 이름 (2026-08 확정)** — 별장·독채는 **리조트 이름을 떼고 각자 이름**을 쓴다.
  본관만 리조트 이름을 쓰는 일본 리조트 구조를 따른 것이다.

  | 실체 | 이름 | 영문 |
  |---|---|---|
  | 본관 트윈 100실 | 야마나미 호텔 | YAMANAMI HOTEL |
  | 8인 별장 1동 | **프레스티지 하우스** | PRESTIGE HOUSE |
  | 2인 별장 3동 | **히노키 캐빈** | HINOKI CABIN |
  | 4인 독채 1동 | **소보 패밀리** | SOBO FAMILY |
  | 2인 독채 10동 | **쿠주 레지던스** | KUJU RESIDENCE |
  | 2인 돔형 4동 | **돔스테이** | — |

  - 형태는 **「고유명 + 종류」 두 낱말**로 맞춘다. 낱말 수가 같아야 표에 나란히 놓았을 때 읽힌다.
  - **이름에 재료·사실을 박지 않는다.** 예전 「히노키빌라」가 캐빈(편백 맞음)과 패밀리(편백 아님)를
    한 브랜드로 묶어 절반이 거짓이 됐다. **편백 표기는 히노키 캐빈에만** 쓴다.
  - **「별장」은 이름에 쓰지 않는다.** 다섯 곳이 전부 별장이라, 하나에만 붙이면 나머지가
    별장이 아닌 것으로 읽힌다. 분류는 설명(`lg-desc`)에서 말한다.
  - 내부 키(`sobo_villa`·`sobo5`·`kuzu`·`dome_house`·`aso_villa`)는 **바꾸지 않는다** —
    엠클릭 상품명 매칭이 「소보별장」·「통나무」·「쿠주힐즈」로 걸려 있다.
  - **사내 도구(`merittour-tools`)는 아직 옛 이름이다.** 리조트 마스터 `name` 은
    `mt_block_override` 의 기본키(hotel)로도 쓰여, 바꾸면 저장된 웹오픈 수량 조정이
    옛 이름으로 떠돈다. 이관 SQL과 함께 별도로 진행한다.
- **문장은 회원 시점으로 쓴다.** 「메리트투어가 운영하는/엄선한 ~」·「저희가 ~」 대신
  「회원님께서 이용하실 수 있는 ~」처럼 회원이 주어가 되게 한다.
  예외 = `/about/`(회사소개)의 **브랜드 정의문**과 **계약 관계 설명**(총판·대리 송금 등)은 회사가 주어여야 맞다.
- 연락처: ㈜메리트투어 02-365-9800 (평일 09:00~18:00).

## 2027 회원 예약 개편 (2026-08 진행 상황)

### 목표와 방향
- **엠클릭(예약 시스템 업체)에 기존 예약 사이트 개편을 요청** 중. 메일 발송 완료, 회신 대기.
- **API 연동은 안 묻기로 함(Min)** → 엠클릭이 자기 시스템에 `/reserve/` 화면을 **이식**하는 방향.
- 핵심 요청 = **상품을 줄이는 것이 아니라 화면만 개선**. 손님에게는 숙소 단위로 하나로 보이고, 출발지·기간 선택에 따라 뒤에서 상품이 자동 매칭되게.
- 엠클릭에 보내도 되는 링크 = **`/reserve/` + `/2027/`** (실제 개인정보 없음·noindex·spec 링크 없음 확인). **`/reserve/spec/`은 보내지 않는다.**

### 엠클릭 현황 (스크린샷 확인)
- **이미 있는 것** — 룸타입·베드타입(TWN/QUAD/SGL/DBL)·**날짜별 블록수/사용수/잔여수**·룸타입별 판매가. 「N실 남음」은 이걸 읽으면 된다. 새로 만들 것 없음.
- **추가로 필요한 필드 4개** — ①룸타입 온라인 노출 on/off(야마나미 소형트윈 제외용) ②출발 가능 요일 ③온라인 판매 구분 3단계(일반/기획전/문의전용) ④예약 인원 하한·상한.
- **묶는 기준은 상품명이 아니라 「대표호텔」 코드**로 할 것. 상품명 파싱은 반드시 어긋난다(우리 `aliases` 12개·`roomAlias` 38곳이 그 증거).
- 상품 수: 현재 124건 → `/reserve/` 기준 재구성 시 80건(숙소15 × 출발지 × 박수). 출발지를 상품 옵션으로 두면 41건이나 엠클릭 구조 변경 필요 → **80건 유지 권장**.
- **2027 이름 통일 예정(Min)** — 엠클릭 호텔명·룸타입명을 우리 리조트 마스터 이름에 맞춘다. 맞추면 `aliases` 부채가 사라진다. **관내별장은 야마나미 호텔에 합치는 것으로 확정**(블록수도 합산해야 보유가 맞음). **스가다이라 고원 온천호텔은 그대로**(숙소=고원 온천호텔 / 골프장=그린CC, 서로 다른 이름).

### `/reserve/` 구현된 것
- **룸타입 선택** — 날짜 고르면 룸타입별 잔여 표시 후 선택. 1종이면 자동 선택. 마감일은 대기 신청이라 건너뜀. 야마나미=트윈 100실 단일(소형트윈 미노출), 포틴힐즈 호텔=트윈13·패밀리룸19(4인실).
- **인원 하한·상한** — `minPaxOf`/`maxPaxOf`. 독채는 동 단위라 정원이 곧 최소(소보 패밀리 4명·프레스티지 8명). 회원권 인원으로 못 잡는 상품은 **버튼 비활성 + 사유 표시 + `pvGo`에서 재차단**.
- **기획전(`PROMO`)** — 일반 목록·검색에서 제외, 오른쪽 상단 「기획전」+N배지로만 진입. `aso_villa`=회원권 무관 예약 가능(8인). `kuzu`(쿠주 레지던스)=**`contactOnly`** — 장기숙박계약 자격을 온라인에서 가릴 수 없어 예약 대신 문의(전화·카카오)로 보냄.
- **차수 차단** — `roundBlock()`이 「자격 없음」과 「휴지 기간」을 구분.
  **특별 회원권은 우선예약 대상이 아니다**(2026-08 변경 · 이전 규칙 「3차만 참여」는 폐기).
  어느 차수에도 배정하지 않고(`eligibleRound` 항상 false → 배정 0회), 우선예약 기간 내내 예약 진행을 막으며,
  자유예약부터 일반 고객과 같은 조건으로 예약한다. 차수를 「해당 없음」으로 늘어놓지 않고
  **「우선예약 대상 아님 · 자유예약 ○○부터」 한 줄**로 보여 준다 — 늘어놓으면 언젠가 자기 차례가 오는 것으로 읽힌다.
  같은 규칙이 `/reserve/spec/` FR-5.2 · `/reserve/book/`(레거시) · `merittour-tools` `tools/booking/`(v15)에
  함께 있다. 한쪽만 고치지 말 것. **`/2027/`(손님 공지문)에서는 특별 회원권 언급을 전부 뺐다** —
  우선예약 안내문인데 대상이 아닌 등급을 적어 두면 참여하는 것으로 읽힌다.
- **시연 5종** — HONG(홍길동·EWRC이용권·4인) / KIM(김영희·특별·2인·**우선예약 대상 아님**) / CHOI(최민창·EWRCⅡ·16인) / PARK(박정우·다이아1·4인) / LEE(이수진·골드·2인·전 차수). 비번=아이디 뒤 휴대폰번호. **로그인 창 = ①회원 선택 → ②차수 선택 → 바로 로그인** + ③아이디 직접 입력.
- **예약 취소** — 없으면 시연을 한 번밖에 못 한다. 취소 시 **그 건이 쓴 차수 횟수까지 복구**(`usedRound`).
- **화면 맞춤** — `fitPanel()`이 실제 높이를 재서 날짜 칸 크기 결정(sticky header 높이 차감). 예약 진행 버튼까지 한 화면에. 폰은 CTA 하단 고정. **달력 접기 토글은 불편해서 롤백**(항상 펼침).
- hover 규칙 12개는 `@media (hover:hover)`로 — 터치에서 잔상이 남아 엉뚱한 버튼에 불이 들어왔다.
- 시즌 = **2027-03 ~ 2027-12**. 우선예약 차수는 2026-10(2026 가을에 2027 출발분 예약).

### `/2027/` 색 — 베이지 라이트 고정 (Min)
- **다크모드 분기 금지.** `@media (prefers-color-scheme:dark)` 를 넣지 않는다. 기기 설정에 따라 같은 안내문이
  갈려 보이고, 직원이 스크린샷을 떠 보낼 때도 사람마다 다른 그림이 나간다. `[data-theme]` 분기도 두지 않는다.
- 팔레트 = `--paper:#F6F2E8` 베이지 · 글자는 올리브/골드 계열 유지(`--ink:#1E2618` `--ink-soft:#4C573F`
  `--muted:#5F6B47` `--gold:#7F6019`) · 표 머리와 안내 박스는 `--band:#EDE7D6` · 회원 패널은 흰 카드 + 골드 테두리.
- **세이지(연한 초록)로 되돌리지 말 것** — `merittour-tools/docs/design_2027_guide_readability.md` §1 은 세이지를
  제안하지만, 바탕이 초록이면 글자(올리브·골드)와 색 계열이 같아 덜 떨어져 보인다는 판단으로 베이지로 바꿨다.
  (같은 문서의 「베이지·골드 계열 비선호」 원칙보다 이 결정이 우선한다)
- 색을 만질 때는 **글자별 대비비를 실제 렌더에서 재고** WCAG AA(본문 4.5) 미달이 0건인지 확인한다.
  현재 **최저 4.62 · 본문 13.96**(574개 요소 전수). `--muted` 는 라벨 50곳(`.facts dt`)을 좌우해 가장 먼저 티가 난다.
  - ⚠ **반투명 배경은 아래 배경과 겹친 색으로 잰다.** 안 겹치면 `.tbd`(「확정 예정」 칩)처럼 띠와 글자가
    같은 색이라 1.00 이 나오거나, 실제 4.47 을 4.62 로 잘못 읽는다. 이 때문에 칩이 한동안 AA 미달로 떠 있었다
    (띠를 12% → 8% 로 낮춰 4.70). `color(srgb 0..1)` 표기도 `rgb()` 로 착각해 읽지 말 것.

### `/2027/` 사진·영상
- 지역마다 **「숙소 정보 보기 / 골프장 정보 보기」**를 접어서 둔다(`.media-fold`).
  파일 자리 = `2027/photos/<지역>/<숙소폴더>|golf/01.webp` 부터 **번호를 이어서**(장수 제한 없음). 규격·올리는 법은 `2027/photos/README.md`.
  **접는 블록은 두지 않는다** — 숙소도 골프장도 항상 보인다. 접어 두면 사진이 있는지조차 눌러 봐야 안다.
- **숙소 사진은 그 숙소 카드 안, 설명 바로 아래에 붙인다.** 접는 블록으로 빼 두면 어느 숙소 이야기인지 눈으로 다시 이어 붙여야 한다.
  카드에 `data-lg="폴더"` 만 붙이면 자리가 생기고, 이름은 `.lg-name` 에서 읽으므로 두 곳에 안 생긴다. **지역 8곳 모두 `<ul class="lodging">` 카드를 갖는다**(예전에는 구마모토만 카드였고 나머지는 `<dt>숙소</dt>` 한 줄이었다).
- **사진 줄은 가로로 민다**(`.shots` flex + `overflow-x`). 세로로 쌓으면 숙소마다 카드가 화면 하나씩 차지한다. 데스크톱은 휠로 가로 이동이 안 되니 `dragStrip` 으로 끌기를 붙였다 — 6px 문턱을 넘긴 뒤에야 끌기로 보고 뒤따르는 클릭 한 번을 삼킨다(안 삼키면 밀었을 뿐인데 크게 보기가 열린다).
- **`.lodging` 은 한 줄에 카드 하나**(`grid-template-columns:1fr`). 두 칸이었을 때는 카드 폭이 절반이라 사진 자리 네 개 중 둘만 보이고 나머지가 잘렸다 — 안 올린 자리를 한눈에 보여 주는 것이 점선의 목적인데 밀어야 보이면 목적을 잃는다.
- **칸 폭은 `clamp(130px, calc((100% - 24px)/4), 190px)`** — 카드 폭에 네 칸이 딱 맞는다(PC 에서 잘리는 줄 0). 폰은 네 칸을 담으면 한 칸이 70px 대로 줄어 무엇인지 안 보이므로, 두 칸을 온전히 보이고 세 번째를 걸치게 둔다(밀 수 있다는 표시).
- **`.lodging li` 에 `min-width:0`.** 그리드 칸의 기본 `min-width:auto` 가 안쪽 가로 스트립의 전체 폭을 그대로 받아 페이지가 통째로 옆으로 밀렸다(900px 화면에서 780px 넘쳤다).
- **칸 크기는 `.shots>*` 하나가 정한다.** `.shots button` 에 `width:100%` 를 또 주면 특이도가 높아 그 값이 이겨, 사진이 들어온 칸만 스트립 전체 폭으로 늘어난다.
- **카드는 접혀 있지 않으므로 접힘에 기대면 안 된다.** 사진은 그 카드가 화면에 가까워질 때(`IntersectionObserver`, 400px 여유) 만든다.
- **장수를 미리 정해 두지 않는다.** `01` 부터 **하나씩 차례로** 두드려(`probe`) 있으면 붙이고 없으면 멈춘다. 한꺼번에 상한만큼 두드리면 24장 × 24자리 = 576번이 되고 대부분 헛걸음이다. 사진이 하나도 없을 때 자리마다 딱 한 번(전체 24번)만 두드린다.
  - 그래서 **번호를 건너뛰면 거기서 끊긴다**(`01`·`03` → `02` 에서 멈춰 `03` 이 안 보인다). README 에 적어 뒀다.
  - 빈 자리는 사진이 없으면 `SHOT_MIN`(4)개, 있으면 **다음 한 자리만**. 이어 붙일 곳만 보이면 되고, 사진 열 장 뒤에 빈 칸 넷이 붙으면 줄만 길어진다.
- **골프장도 숙소와 같이 항상 열려 있다**(`.media-open`). 접기(`<details>`)와 「지나가면 자동으로 접기」는 걷어냈다 — 접히는 것이 없으니 함께 죽은 코드가 된다.
- **빈 칸에는 폴더 이름만 적는다.** 지역까지 붙이면 좁은 칸에서 잘려 `hinoki-cabin` 과 `hinoki-family` 가 둘 다 「hinoki-…」로 보인다. 앞에 붙는 경로는 블록 아래 안내줄에 한 번만.
- 크게 보기는 **그 숙소 사진 안에서만** 넘어간다. 블록 전체를 한 목록으로 묶으면 프레스티지에서 넘겼는데 료칸이 나온다.
- **틀을 감추지 않는다.** 아직 안 올린 자리는 점선으로 두고 **어느 파일을 어디에 넣으면 되는지** 그 자리에 적는다.
  빈 칸을 감추면 올릴 때마다 문서를 다시 뒤져야 한다.
- **접혀 있는 동안 `<img>` 를 만들지 않는다.** 만들어 두면 지역 8곳 × 블록 2개 = 96장을 문서 열 때 받는다.
  `<details>` 의 `toggle` 에서 처음 열릴 때 한 번만 만든다.
- **영상은 저장소에 넣지 않는다.** 4K 한 편이 수백 MB다. 유튜브(미등록) 주소를 `VIDEO` 에 적고,
  **썸네일을 누를 때만** iframe 을 끼운다 — 처음부터 박으면 지역마다 유튜브 스크립트가 딸려 온다.
- 라이트박스는 닫을 때 **누른 자리로 초점을 돌려주고** `body` 스크롤 잠금을 푼다. 안 그러면 뒤 문서가 같이 움직인다.

### `/2027/` 문안 결정 (Min)
- **회원 안내만** — 「일반 고객」 언급 0건. 현지 상황 원칙 제거.
  - **등급별 예약 가능 인원 블록은 되살렸다**(2026-08). 한 번 뺐다가 다시 넣은 자리이니 또 빼지 말 것.
    숫자의 짝은 `/reserve/` 의 `DEMO_ACCOUNTS` 다 — 다이아1·다이아2·EWRC이용권·EWRCⅡ 4인 / 골드 2인.
    **특별 등급은 이 표에 넣지 않는다**(우선예약 대상이 아니다). 특별 회원권 자체는 남아 있으므로
    사내 도구(`tools/dashboard` 인원 기본값 · `tools/library` 회원권 자료)에서는 그대로 둔다.
    한쪽만 고치면 같은 회원권이 두 화면에서 다른 인원으로 보인다.
  - 「한 구좌당 인원 초과 시 초과분 취소」는 이 표와 **한 몸**이다. 표만 되살리고 규칙을 빼면 숫자를 넘겼을 때 어떻게 되는지가 없다.
- 「회원제」 → **「회원 우선 예약제」**. h2 = **「회원님의 한 해를 준비합니다」**(우선·먼저 배정 같은 순위 표현 배제).
- s-01 리드 = **「골프 상품은 모두 회원 우선 예약제로 운영됩니다」**. 「메리트투어 골프 상품은 ~」처럼 회사를 앞세우지 않는다.
- 원칙 3 = **「회원님 예약이 한 해 운영의 기준입니다」**(「미리 예약하시는 것이 유리합니다」로 바꿨다가 되돌림). 근거는 코스 관리만이 아니라 **연간 항공 좌석 확보**를 함께.
- **리조트별 최대 연속 체류일** — 회원권 연결·예약 이어붙이기로 장박 만드는 것 차단. 하루라도 비면 별개. 스가다이라만 14박이라 리조트별 값이 다름. 판정 규칙은 `spec/` §6.0.
- **요금 표기 = 「무엇이 포함된 금액인가」**. 「패키지」·「원화 총액」은 쓰지 않는다(전 상품이 패키지고, 손님은 어차피 원화로 받는다).
- 주류 = **일본 쇼츄(焼酎)** · 프레스티지(8인) 2병 / 소보 패밀리(4인) 1병. 「일본 소주」는 한국 소주를 떠올리게 해서 안 씀. 박수 기준 아님.
- 전기카트 = 아소 야마나미에서 **유료 사전 신청으로 이용 가능**(제한 표현 배제). 프레스티지·소보 패밀리 무료, 그 밖 1일 1대 2,000엔.
- 간지호텔 = **호텔 온천 + 근처 아카가와 온천도 이용 가능(입욕료 별도)**. 「100%」·목장 언급 삭제.
- 스가다이라 온천 24시간·여탕 시간 제한 삭제(운영시간은 바뀌면 안내가 틀린 말이 된다).
- 닐라이스프링스 = **우선예약 대상 아님 · 자유예약 기간 오픈**(블록 머리에 표기). `/reserve/`에는 없는 게 맞다.
- 시즈노야도 「약 700년 만간지 천연온천」 = **사실 확인 완료**(남오구니초 공식·구마모토현 관광 자료).
- 포틴힐즈 「식사 업그레이드·추가」 블록 제거 — 상추 500엔까지 있어 예약 안내에 과했다. 확정서·출발 전 안내로.

### 남은 일
1. **엠클릭 회신 대기** — 이식 방향.
2. **`/2027/` 미확정 3곳** — 무츠키·스가다이라 요금 / 닐라이스프링스 / 예약 변경·취소 규정. **손님 공지 전 필수**.
3. 2027 지역 사진 — 올리지 않기로 함(없으면 스트립이 자동으로 사라짐).
4. `merittour-tools` Supabase 04~07 + 첫 owner **적용 완료**(확인함). 콘솔에만 있는 마이그레이션 08~11이 저장소에 없음 — 새 환경 구성 시 빠짐.
5. 대시보드 환율 기준일 = **2026년 출발 2달 전 / 2027년 출발부터 3달 전**(정정 완료).
